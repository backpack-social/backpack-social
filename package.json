{
  "name": "backpack-social",
  "private": true,
  "scripts": {
    "build": "pnpm dlx turbo run build",
    "dev": "pnpm dlx turbo run dev --concurrency 12",
    "lint": "pnpm dlx turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.1.2"
  },
  "packageManager": "pnpm@9.12.1",
  "engines": {
    "node": ">=18"
  }
}